# Ergebnisse

## Bilder

```{r, fig.cap= "a plot", fig.pos='h'}
plot(mtcars$mpg, mtcars$cyl)
```

some boring text

## Tabellen

```{r, fig.pos='h'}
kableExtra::kable(head(mtcars), caption = "A table") %>% 
  kableExtra::kable_styling(latex_options = c("hold_position"))
# 
# n_valid <- function(x) {sum(!is.na(x))}
# 
# full1 <- as.list(sample_data[ , vec_leadership])
# filter1 <- as.list(filtered_data[ , vec_leadership])
# 
# map2(full1, filter1, n_valid)

```

## Mehr Grafiken

```{r}
ggplot(drop_na(palmerpenguins::penguins), 
       aes(x = bill_depth_mm, y = bill_length_mm, color = species)) +
  geom_point() +
  facet_wrap(~sex) +
  scale_color_brewer(palette = 3, type = "qual") +
  labs(caption = "data source: palmerpenguins-package") +
  theme_minimal() +
  theme(legend.position = "bottom")
```

<!-- the last line needs to specify the \newpage-command -->

\newpage
