
% allow language specific section/table-headers/captions
\usepackage[german]{babel} % adapt 'german' to either 'french' or 'italian'
\usepackage[german=quotes]{csquotes} % adapt 'german' to either 'french' or 'italian'

% change font to Arial
\usepackage{fontspec}
\setmainfont{Arial}

% ensure multi-page tables with repeating captions
\usepackage{longtable}

\usepackage{float}

% help caption figures and tables
\usepackage{caption}

% comment this out in production - shows the page margins
\usepackage{showframe}

% define page geometry
\geometry{a4paper, bottom=40mm}

% specify tabluarx-tables: this allows for the tables have different column-widths
% set column widths for tabularx
\usepackage{tabularx}
\newcolumntype{s}{>{\hsize=.7\hsize}X}
\newcolumntype{b}{X}

% Header with logo and footer with page numbers
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{} % sets both header and footer to nothing
\addtolength{\headheight}{38pt} % make more space for the header
% \addtolength{\headwidth}{-20pt} % making the headwidth smaller (pushes left logo in / out)
\pagestyle{fancyplain} % use fancy for all pages except chapter start
\lhead{\includegraphics[height=1.2cm]{layout/INS-logo-placeholder.png}} % left logo
\rhead{\includegraphics[height=1.2cm]{layout/project-logo-placeholder.png}} % right logo
\renewcommand{\headrulewidth}{0pt} % remove rule below header

% Set the head width to be the full page width
\newlength{\oddmarginwidth}
\setlength{\oddmarginwidth}{1in+\hoffset+\oddsidemargin}
\newlength{\evenmarginwidth}
\setlength{\evenmarginwidth}{\evensidemargin+1in}
\fancyhfoffset[LO,RE]{\oddmarginwidth}
\fancyhfoffset[LE,RO]{\evenmarginwidth}

% define landscape mode to optionally use when having broad tables
\usepackage{pdflscape}
\newcommand{\blandscape}{\begin{landscape}}
\newcommand{\elandscape}{\end{landscape}}

% Allow inclusion of other PDFs
\usepackage{pdfpages}

% % Following below
% turns maketitle off, then you can define your own titlepage in before_body.tex
\let\oldmaketitle\maketitle
\AtBeginDocument{\let\maketitle\relax}
